# Database
DATABASE_URL="file:./dev.db"

# JWT Secret for session auth (change in production!)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Cohere AI Configuration (for AI-enhanced content generation)
# Get your API key at https://dashboard.cohere.ai/api-keys
COHERE_API_KEY="your-cohere-api-key"

# Email Configuration (choose one provider)
# Options: 'sendgrid', 'resend', or 'console' (for development)
EMAIL_PROVIDER="console"
EMAIL_FROM="noreply@poweredbyape.ai"

# SendGrid (if using SendGrid)
# SENDGRID_API_KEY="your-sendgrid-api-key"

# Resend (if using Resend)
# RESEND_API_KEY="your-resend-api-key"

# Google Calendar OAuth (for calendar integration)
# GOOGLE_CLIENT_ID="your-google-client-id"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"
